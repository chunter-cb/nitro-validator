{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "hardhat",
            "request": "launch",
            "name": "Debug Solidity Test",
            "task": "test",
            "args": ["${relativeFile}"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}